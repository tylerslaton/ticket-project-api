let sample = require('../models/sample.js');

/*
 * GET /sample route to retrieve all the samples.
 */
function getSample(req, res) {
}

/*
 * POST /sample to save a new sample.
 */
function postSample(req, res) {
}

/*
 * GET /sample/:id route to retrieve a sample given its id.
 */
function getSample(req, res) {
}

/*
 * DELETE /sample/:id to delete a sample given its id.
 */
function deleteSample(req, res) {
}

/*
 * PUT /sample/:id to updatea a sample given its id
 */
function updateSample(req, res) {
}

//export all the functions
module.exports = { getSamples, postSample, getSample, deleteSample, updateSample };